import { DISTRICT, DISTRICT_TYPES, NEIGHBOURHOOD, ENCOUNTER } from './map'

const SCENARIO = new Map()

const M = 'M'
SCENARIO.set(
  M, {
    name: M,
    description: 'This a demo scenario',
    map: []
  }
)
